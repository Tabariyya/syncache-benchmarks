services:
  redis:
    image: redis:7-alpine
    ports:
      - "23379:6379"
    command: >
      redis-server
      --save ""
      --appendonly no
      --hz 100
      --dynamic-hz yes
      --no-appendfsync-on-rewrite yes
      --loglevel warning

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: benchmark
    ports:
      - "49432:5432"
    command: >
      postgres
      -c fsync=off
      -c synchronous_commit=off
      -c full_page_writes=off
      -c checkpoint_completion_target=0.9
      -c wal_level=minimal
      -c max_wal_senders=0
      -c shared_buffers=256MB
      -c work_mem=64MB
      -c effective_cache_size=512MB
      -c random_page_cost=1.1
      -c logging_collector=off

  benchmark:
    build: .
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - redis
      - postgres
    env_file:
      - ../.env
    environment:
      SYNCACHE_TOKEN: "eyJhbGciOiJFUzI1NiJ9.eyJicm9rZXJVUkwiOiJ3c3M6Ly9icm9rZXIuc3luY2FjaGUudGFiYXJpeXlhLmNvbS8iLCJjb21wYW55TmFtZSI6IlRlc3R0dHR0dCIsImluc3RhbmNlTnVtYmVyIjo1LCJwcm9qZWN0TmFtZSI6InRlc3R0dHR0IiwiZXhwIjoxODEwNzE2MTU1LCJ0eXBlIjoiSU5TVEFOQ0UiLCJpYXQiOjE3NzkxODAxNTV9.4yBzO_UUOt6V8ORoPlxTzR0y0x5KDwVh1NRlfGRDIwI5ff9vFsQOj1kuB0l2YproR1FH_hweWUmknQHz1oNx5w"
