version: "3.9"

services:
  app:
    build: .
    container_name: main_app
    depends_on:
      - redis

  redis:
    image: redis:latest
    container_name: redis_server
    ports:
      - 6379:6379


